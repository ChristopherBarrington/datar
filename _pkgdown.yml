url: https://christopherbarrington.github.io/datarepository

destination: docs

reference:
  - title: Saver
    desc: These functions use `base::save` to write objects to a data repository. The `base::save` function will be used if `save(file='file.rda', ...)` is specified otherwise the data repository is used.
    contents:
      - save
  - title: Paths
    desc: The functions here define where the data repository can be found and create the 'data' directory and intermediates.
    contents:
      - init
      - system.file
  - title: Loader
    desc: These functions are used to load data from a 'data' directory using `utils::data` behind the scenes. I don't know why it says the function is called `...` it should be `data()`!
    contents:
      - data
